'use client';

/**
 * ScrollProgressBar is now disabled to provide a cleaner UI.
 * Reading progress is now integrated into the Table of Contents (TOC) component.
 */
export const ScrollProgressBar = () => {
  return null;
};
